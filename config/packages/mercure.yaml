mercure:
    hubs:
        default:
            url: '%env(MERCURE_URL)%'
            public_url: '%env(VITE_MERCURE_PUBLIC_URL)%'
            jwt:
                secret: '%env(MERCURE_JWT_SECRET)%'
                publish: '*'
